(comment
  function that returns a contract
  and use that contract in a contract signature

  instead of hardcoding gt10 lt15, use gt lt and provides argument
  we can make complex contract by composing expressions
)

(val rec gt = (lambda v (lambda x (x >= v))))
(val rec lt = (lambda v (lambda x (if (x >= v) then #f else #t))))
(val rec and = (lambda x (lambda y (if x then y else #f))))
(val rec or = (lambda x (lambda y (if x then #t else y))))

(val rec g
  : ((contract (gt 10)) -> (contract (lambda x ((and ((gt 10) x)) ((lt 15) x)))))
  = (lambda x (x - 10)))

(g 11)
