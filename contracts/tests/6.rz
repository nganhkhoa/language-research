(comment dependent contract)

(val rec gt100 = (lambda x (x >= 100)))

(comment
  g accepts everything
  but on output, it also check with the input if input + output >= 100

  g = lambda x. x + 80

    g 5 -> 85, 5 + 85 < 100
    g 10 -> 90, 10 + 90 >= 100
)

(val rec g
  : ((contract (lambda x #t)) d-> (lambda x (contract (lambda result (gt100 (x + result))))))
  = (lambda x (x + 80)))
(g 10)
